{
  "name": "nota-filler-root",
  "private": true,
  "scripts": {
    "format": "echo no formatter specified && exit",
    "lint": "echo no linter specified && exit 1",
    "lint:fix": "pnpm run lint -- --fix",
    "dev": "pnpm run --filter @nota-filler/app dev -- --host",
    "build": "pnpm run --filter @nota-filler/app build"
  }
}
